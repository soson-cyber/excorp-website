# EX-LINK 다이어그램 — QA 검증 보고 (qa-verifier)

날짜: 2026-06-05
대상: `src/components/solution/ExlinkDiagram.tsx` (XR Solution §03 Architecture 통합)

## 결과: **PASS**

전 항목 통과. 차단 이슈 없음.

---

## 1. 타입 / 린트
| 검사 | 명령 | 결과 |
|------|------|------|
| 타입 | `npx tsc --noEmit` | exit 0 (오류 없음) |
| 린트 | `npx eslint ExlinkDiagram.tsx page.tsx` | exit 0 (경고/오류 없음) |

## 2. 통합 확인
- `src/app/solution/xr-solution/page.tsx`
  - L9 `import { ExlinkDiagram }`, L184 `<ExlinkDiagram />` 가 §03 Architecture(L154~) 내부에 렌더.
  - 상단 흐름 텍스트 "CAPTURE → TRACKING → EXLINK CORE → RENDER → BROADCAST"(L162) 유지됨.
- 라우트 `/solution/xr-solution` HTTP 200, 서버 에러 없음(preview_logs error=none).
- 프리뷰 생명주기: 기존 :3000 프로세스 full stop → `rm -rf .next` → fresh preview_start 수행.

## 3. 15개 라벨 실측(DOM, SVG `<text>`)
| 뷰포트 | present | missing | 중복 | 비고 |
|--------|---------|---------|------|------|
| 데스크톱(1320) | 15/15 | [] | 없음 | 추가 텍스트는 INPUT/OUTPUT 캡션뿐 |
| 모바일(375) | 15/15 | [] | 없음 | 동일 |

검증된 15개(철자 그대로): EX-LINK · VP Production Core · Camera · Tracking · Controller · Media Server · Aximmetry · Unreal Engine · Video I/O · Control System · LED Wall · Chroma Key · Broadcast · Recording · Streaming. (v3 누락 항목 Recording 포함 확인.)

## 4. 반응형
- 데스크톱(1320): `div.hidden.sm:block` display=block, 모바일 div=none. `document.scrollWidth - clientWidth = 0`(가로 오버플로우 없음).
- 모바일(375): 모바일 div display=block, 데스크톱 div=none. **`scrollWidth(375) == clientWidth(375)` — 가로 스크롤 없음.** 세로 스택(INPUT row → CORE → OUTPUT column) 정상.

## 5. 브랜드 색 정합(육안, 스크린샷)
- 코어 큐브: 퍼플(#5e2ec0 계열) + 시안/민트 글로우 엣지. **노랑/주황 없음.**
- 입력 연결선: 시안/민트. 출력 연결선: 바이올렛.
- Chroma Key 칩: 클린 그린(체크 아이콘). LED Wall 칩: 블루. Recording: 레드 점. Broadcast/Streaming: 다크 퍼플 + 바이올렛 스트로크.

## 6. 모션 / reduced-motion
- 파티클: SMIL `animateMotion`×12 + `mpath`×12(데스크톱 8 + 모바일 4) 존재, JS 불필요.
- reduced-motion 규칙 존재·정상 스코프 확인:
  `@media (prefers-reduced-motion: reduce){ .exlink-diagram .exlink-particles{ display:none } }`
- 결정적 검증: 동일 셀렉터를 무조건 규칙으로 주입 시 → 파티클 그룹 `display:none`, 연결선(`use` inline)·라벨(`text` block)은 그대로 유지. 셀렉터가 파티클만 정확히 타깃함을 확인.
  - (주: 헤드리스 프리뷰 도구는 colorScheme만 에뮬레이션하고 reduced-motion 라이브 에뮬은 미지원 → 위 주입 방식으로 동등 검증.)

## 증거 스크린샷
- 데스크톱(1320): 다이어그램 전체 — 좌 입력 삼각 / 중앙 EX-LINK 코어(내부 5칩 격납) / 우 출력 5. (본 세션 첨부)
- 모바일(375): 세로 스택, 라벨 가독 양호, 가로 스크롤 없음. (본 세션 첨부)

## DoD 대조
15라벨 정확 ✅ · 브랜드 색 정합 ✅ · 파티클 + reduced-motion 정지 ✅ · tsc/eslint ✅ · 데스크톱/모바일 렌더 ✅ · §03 통합 ✅ → **전부 충족.**
